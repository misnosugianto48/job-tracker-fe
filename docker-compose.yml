services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-/api}
    container_name: job_tracker_frontend
    restart: always
    ports:
      - "127.0.0.1:3000:80"
